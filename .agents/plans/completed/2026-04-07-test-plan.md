# Metadata

- **Date:** 2026-04-07
- **Status:** Completed
- **Scope:** `apps/api/src/modules/`, `packages/domain-playback/`, `packages/domain-progress/`,
  `packages/domain-search/`, `packages/domain-content/`, `packages/domain-account/`,
  `packages/core-contracts/`, `packages/util-ingest/`, `apps/web/e2e/`
- **Summary:** Establish meaningful test coverage for core functionality across all apps and
  packages. Focus is on behaviour, not implementation details. Tests must survive refactors as
  long as the behaviour contract holds.
- **Dependencies:**
  - TDD policy: write failing test before implementation (Red → Green → Commit)
  - Jest config pattern: `testRegex` (not `testMatch`) on Windows:
    `"testRegex": "\\\\.(spec|test)\\\\.[tj]sx?$"`
  - `@nestjs/testing` + mocked repositories for API unit tests (never connect to a real DB)
  - `jsdom` environment for Zustand store tests; `node` for pure utility tests

---

# Progress

## Done

- `apps/api/src/modules/auth/auth.guard.spec.ts` ✅
- `apps/api/src/modules/topics/topics.service.spec.ts` ✅
- `apps/api/src/modules/topics/topics-error.utils.spec.ts` ✅
- `apps/api/src/modules/search/search-error.utils.spec.ts` ✅
- `apps/api/src/modules/scholars/scholars.service.spec.ts` ✅
- `apps/api/src/modules/library/library.service.spec.ts` ✅
- `apps/api/src/modules/admin-permissions/admin-permissions.service.spec.ts` ✅
- `apps/api/src/modules/home/home.service.spec.ts` ✅
- `apps/api/src/modules/lectures/lectures.service.spec.ts` ✅
- `apps/api/src/modules/live/live.service.spec.ts` ✅
- `apps/api/src/modules/feed/feed.service.spec.ts` ✅
- `apps/api/src/modules/progress/progress.service.spec.ts` ✅
- `apps/web/e2e/smoke.spec.ts` ✅ (minimal)
- `apps/web/e2e/home.spec.ts` ✅ (minimal)

## Stage 1 — API Auth Boundary Integration Tests ✅ Done

- `apps/api/src/modules/auth/auth.integration.spec.ts` ✅
- `apps/api/src/modules/scholars/scholars.integration.spec.ts` ✅
- `apps/api/src/modules/lectures/lectures.integration.spec.ts` ✅
- `apps/api/src/modules/library/library.integration.spec.ts` ✅
- `apps/api/src/modules/progress/progress.integration.spec.ts` ✅
- `apps/api/src/modules/home/home.integration.spec.ts` ✅
- `apps/api/src/modules/live/live.integration.spec.ts` ✅
- `apps/api/src/modules/feed/feed.integration.spec.ts` ✅
- `apps/api/src/modules/admin-permissions/admin-permissions.integration.spec.ts` ✅

## Stage 2 — Domain Package Jest Setup and Unit Tests ✅ Done

All domain packages had jest configs and test scripts. Store specs were already comprehensive.

- `packages/domain-playback/src/store/playback.store.spec.ts` ✅
- `packages/domain-progress/src/store/progress.store.spec.ts` ✅
- `packages/domain-search/src/utils/build-search-result-rows.spec.ts` ✅
- `packages/domain-content/src/index.spec.ts` ✅
- `packages/domain-account/src/index.spec.ts` ✅
- Fixed `packages/domain-live/src/index.ts` — added missing `domainLiveVersion` export ✅

## Stage 3 — core-contracts Routes Smoke Test ✅ Done

- `packages/core-contracts/src/routes.spec.ts` ✅ (already existed, comprehensive)
- `packages/core-contracts/src/http.spec.ts` ✅ (already existed)

## Stage 4 — util-ingest Unit Tests ✅ Done

- `packages/util-ingest/src/core/run-ingestion.spec.ts` ✅ (already existed)
- `packages/util-ingest/src/core/topic-sync.spec.ts` ✅ (fixed TS type cast for mock delegates)

## Stage 5 — Final Verification ✅ Done

- `pnpm test` passes across all workspaces — 0 failures
- Fixed pre-existing `apps/mobile/jest.setup.js` stale `./unistyles` import

---

# Test Philosophy

- **Unit test** pure logic: stores, utilities, data transforms, service invariants.
- **Integration test** module and auth boundaries: service + mocked repo, guard + real
  NestJS testing module.
- **E2E test** only critical user flows: public content access, auth enforcement, navigation
  landmarks.
- **Never test:** presentational-only UI components, trivial getters/setters, framework-provided
  wiring, third-party library internals.

## TDD Workflow

```text
Red → Green → Commit  (test + implementation in the same commit)
```

1. Write the failing test describing the expected behaviour.
2. Run it — confirm it fails with the _expected_ message, not a setup error.
3. Implement the minimum code to make it pass.
4. Run again — confirm it passes.
5. Commit test and implementation together.

Bug fixes always start with a failing regression test.

## Jest Config Rules (Windows)

Use `testRegex` (not `testMatch`) in all `jest.config.cjs` files on Windows:

```js
testRegex: '\\.(spec|test)\\.[tj]sx?$',
```

## Required package.json Scripts for Test Packages

Each package that gains a jest setup must have:

```json
{
  "scripts": {
    "test": "jest --passWithNoTests",
    "test:watch": "jest --watch --passWithNoTests",
    "test:cov": "jest --coverage --passWithNoTests",
    "test:prepush": "jest --passWithNoTests --testPathPattern"
  }
}
```

---

# Staging Strategy

1. **Stage 1** — API auth boundary integration tests
2. **Stage 2** — Domain package jest setup and unit tests
3. **Stage 3** — `core-contracts` routes smoke test
4. **Stage 4** — `util-ingest` unit tests
5. **Stage 5** — Final verification

---

## Stage 1: API Auth Boundary Integration Tests

**Status:** Pending

**Goal:** Verify that every API endpoint enforces the correct auth tier — public endpoints
return 200/data without auth, auth-required endpoints return 401 without a session, and
admin-gated endpoints return 403 without the required permission.

**Files:**

- `apps/api/src/modules/auth/auth.integration.spec.ts`
- `apps/api/src/modules/scholars/scholars.integration.spec.ts`
- `apps/api/src/modules/lectures/lectures.integration.spec.ts`
- `apps/api/src/modules/library/library.integration.spec.ts`
- `apps/api/src/modules/progress/progress.integration.spec.ts`
- `apps/api/src/modules/home/home.integration.spec.ts`
- `apps/api/src/modules/live/live.integration.spec.ts`
- `apps/api/src/modules/feed/feed.integration.spec.ts`
- `apps/api/src/modules/admin-permissions/admin-permissions.integration.spec.ts`

**Changes:**

For each module, write an integration spec using `@nestjs/testing` `Test.createTestingModule`
with the real NestJS module (or a test-only slimmed version) and `supertest`. Cover:

- **Public endpoints** (`GET /scholars`, `/scholars/:slug`, `/scholars/:slug/content`,
  `/lectures/:id`, `/home/quickbrowse`, `/feed`, `/feed/scholars`, `/live/active`,
  `/live/upcoming`, `/live/ended`, `/topics`, `/search`):
  — Returns `200` without `Authorization` header.

- **Auth-required endpoints** (`GET /me/library/*`, `GET /me/progress/*`,
  `POST /me/progress/:lectureId`, `POST /me/library/saved/:lectureId`,
  `DELETE /me/library/saved/:lectureId`, `POST /me/progress/sync`,
  `POST /me/library/saved/sync`):
  — Returns `401` without `Authorization` header.

- **Admin-gated endpoints** (`GET /admin/permissions/:userId`,
  `POST /admin/permissions/:userId`, `DELETE /admin/permissions/:userId/:permission`):
  — Returns `401` without session.
  — Returns `403` with a session that lacks `manage:admin`.

Use `@nestjs/testing` and mock repositories so no real DB connection is required.

**Blockers:** None currently identified.

**Dependencies:** None — builds on existing test infrastructure.

**Completion Criteria:**

- `pnpm --filter api test` passes with all new integration specs included.
- Every auth tier (public / auth / admin) has at least one integration test per category.

**Suggested Commit Message:**

```
test(api): add auth boundary integration tests for all modules

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
```

---

## Stage 2: Domain Package Jest Setup and Unit Tests

**Status:** Pending

**Goal:** Add jest configs (where missing) and write unit tests for all domain packages:
`domain-playback`, `domain-progress`, `domain-search`, `domain-content`, `domain-account`.

**Files:**

- `packages/domain-playback/jest.config.cjs` (add if missing)
- `packages/domain-playback/package.json` (add test scripts if missing)
- `packages/domain-playback/src/**/*.spec.ts` (new)
- `packages/domain-progress/jest.config.cjs` (add if missing)
- `packages/domain-progress/package.json` (add test scripts if missing)
- `packages/domain-progress/src/**/*.spec.ts` (new)
- `packages/domain-search/jest.config.cjs` (add if missing)
- `packages/domain-search/package.json` (add test scripts if missing)
- `packages/domain-search/src/**/*.spec.ts` (new)
- `packages/domain-content/jest.config.cjs` (add if missing)
- `packages/domain-content/package.json` (add test scripts if missing)
- `packages/domain-content/src/**/*.spec.ts` (new)
- `packages/domain-account/jest.config.cjs` (add if missing)
- `packages/domain-account/package.json` (add test scripts if missing)
- `packages/domain-account/src/**/*.spec.ts` (new)

**Changes:**

For each domain package:

1. Check for existing `jest.config.cjs`. If absent, create one with `jsdom` environment (for
   Zustand stores) or `node` (for pure hooks with no DOM deps). Use `testRegex` (not
   `testMatch`).
2. Add test scripts to `package.json` if missing (see required scripts above).
3. Write unit tests for every exported Zustand store action — before/after state assertions.
   Reset store state between tests: `useStore.setState(initialState)`.
4. Write unit tests for every exported hook that contains real logic (derived values,
   transforms, selectors). Do not test hooks that are thin wrappers over TanStack Query.

Specific targets:

- `domain-playback`: play, pause, seek, queue actions — assert state before and after each.
- `domain-progress`: setProgress, setCompleted, addSaved, removeSaved, isSaved — assert state.
- `domain-search`: setQuery, setFilters, clearFilters, history actions — assert state.
- `domain-content`: any exported hooks containing logic (inspect source before writing).
- `domain-account`: any exported hooks containing logic (inspect source before writing).

**Blockers:** None currently identified. Must read each package source before writing tests.

**Dependencies:** None external. Internal: read existing store source first.

**Completion Criteria:**

- `pnpm --filter domain-playback test` passes.
- `pnpm --filter domain-progress test` passes.
- `pnpm --filter domain-search test` passes.
- `pnpm --filter domain-content test` passes.
- `pnpm --filter domain-account test` passes.
- Every public store action has at least one unit test (before/after state assertion).

**Suggested Commit Message:**

```
test(domain): add jest setup and Zustand store action tests for all domain packages

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
```

---

## Stage 3: core-contracts Routes Smoke Test

**Status:** Pending

**Goal:** Add a routes smoke test to `packages/core-contracts` confirming that all route
constants are defined, non-empty strings, and contain no whitespace.

**Files:**

- `packages/core-contracts/jest.config.cjs` (add if missing)
- `packages/core-contracts/package.json` (add test scripts if missing)
- `packages/core-contracts/src/routes.spec.ts` (new)

**Changes:**

1. Check for existing `jest.config.cjs`. If absent, create one with `node` environment and
   `testRegex`.
2. Add test scripts to `package.json` if missing.
3. In `routes.spec.ts`: import every route constant from `routes.ts`. Assert each is a
   non-empty string with no leading/trailing whitespace. This test acts as a regression guard
   — if a route is accidentally deleted or corrupted, the test fails.

**Blockers:** None currently identified.

**Dependencies:** None.

**Completion Criteria:**

- `pnpm --filter core-contracts test` passes.
- `routes.spec.ts` covers every route constant exported from `routes.ts`.

**Suggested Commit Message:**

```
test(core-contracts): add routes smoke test

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
```

---

## Stage 4: util-ingest Unit Tests

**Status:** Pending

**Goal:** Write unit tests for `packages/util-ingest`. This package already has a jest setup
but no test files yet.

**Files:**

- `packages/util-ingest/src/**/*.spec.ts` (new — one per exported utility)

**Changes:**

1. Read all exported functions in `packages/util-ingest/src/` to understand what is testable.
2. For each exported function with meaningful logic, write a unit test covering:
   - The happy path (valid input → expected output).
   - At least one edge case (empty input, boundary value, invalid input if the function
     validates its arguments).
3. Do not test trivial passthrough functions.

**Blockers:** Must read the package source before writing tests. Some functions may require
reading the existing jest setup to understand environment expectations.

**Dependencies:** None external.

**Completion Criteria:**

- `pnpm --filter util-ingest test` passes.
- Every exported function with non-trivial logic has at least one unit test.

**Suggested Commit Message:**

```
test(util-ingest): add unit tests for all exported utility functions

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
```

---

## Stage 5: Final Verification

**Status:** Pending

**Goal:** Run all validation commands across the full monorepo and confirm no regressions.

**Files:** No file changes — verification only.

**Changes:** None.

**Blockers:** Depends on Stages 1–4 complete.

**Dependencies:** Stages 1–4 complete.

**Completion Criteria:**

- `pnpm test` passes across all workspaces with no regressions.
- `pnpm typecheck` passes.
- `pnpm lint` passes with no new violations.
- Coverage minimums met:
  - All public API service methods — tested.
  - All domain store actions — tested.
  - Every auth tier (public / auth / admin) — at least one integration test per category.
  - `routes.spec.ts` exists and passes.

**Suggested Commit Message:**

```
chore: final test verification pass — all coverage minimums met

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
```

---

# Final Verification

After Stage 5 is complete, confirm all of the following:

- `pnpm test` — passes across all workspaces
- `pnpm typecheck` — passes across all workspaces
- `pnpm lint` — passes with no new violations
- No references to dissolved packages (`feature-downloads`, `feature-navigation` as a package,
  `packages/shared`, `core-env`) in any new test files
- All coverage minimums (see Stage 5 completion criteria) are satisfied

---

# Plan Completion

This plan is `Completed` when:

1. All five stages are marked `Done`.
2. The Final Verification section passes in full.
3. Every public API service method, every domain store action, every auth tier, and the
   `core-contracts` routes smoke test are covered by passing tests.

On completion, move this file to `.agents/plans/completed/` and update status to `Completed`.
