/** Exposes the web authentication client, session user type, and authentication hook. */
export { authClient } from "./auth-client";
export type { User } from "./auth-client";
export { useAuth } from "./use-auth";
