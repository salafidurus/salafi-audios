import { DevPage } from "@/shared/components/dev-page/dev-page";

export default function SignupPage() {
  return (
    <DevPage
      title="Sign up"
      description="Account creation is being prepared. This page will guide new listeners through setup."
    />
  );
}
