{
  // Editor / typecheck only — does not emit. NestJS CLI uses tsconfig.build.json.
  "extends": "@sd/util-config/tsconfig/nest.json",
  "compilerOptions": {
    "noEmit": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src/**/*.ts", "test/**/*.ts"]
}
