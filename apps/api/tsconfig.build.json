{
  // Build config — drives NestJS CLI compilation and produces the dist/ output.
  "extends": "../../tsconfig.nest.json",
  "compilerOptions": {
    "declaration": false,
    "removeComments": true,
    "sourceMap": true,
    "outDir": "./dist",
    "incremental": true,
    "noFallthroughCasesInSwitch": true,
    "noEmit": false
  },
  "exclude": ["node_modules", "dist", "test", "**/*.spec.ts"],
  "include": ["src/**/*.ts"]
}
