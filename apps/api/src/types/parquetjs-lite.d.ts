/** Local type boundary for the pinned CommonJS Parquet writer dependency. */
declare module 'parquetjs-lite' {
  /** Runtime schema constructor supplied by parquetjs-lite. */
  export const ParquetSchema: any;
  /** Runtime writer constructor supplied by parquetjs-lite. */
  export const ParquetWriter: any;
}
