import {
  UpdateListingDetailsDtoSchema,
  type UpdateListingDetailsDto as UpdateListingDetailsDtoType,
} from '@sd/core-contracts';

/** Request contract for changing the editable metadata of a listing. */
export { UpdateListingDetailsDtoSchema };
/** Fields accepted by the listing-details update endpoint. */
export type UpdateListingDetailsDto = UpdateListingDetailsDtoType;
