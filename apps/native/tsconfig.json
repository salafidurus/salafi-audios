{
  "extends": ["expo/tsconfig.base", "../../tsconfig.packages.json"],
  "include": ["**/*.ts", "**/*.tsx", ".expo/types/**/*.ts", "expo-env.d.ts"],
  "compilerOptions": {
    // react-jsx uses the automatic runtime (React 17+) — no manual React import needed
    "jsx": "react-jsx",
    "types": ["jest", "node"],
    "moduleSuffixes": [".native", ".web", ""],
    "paths": {
      "@/*": ["./src/*"],
      "@/assets/*": ["./assets/*"]
    }
  }
}
