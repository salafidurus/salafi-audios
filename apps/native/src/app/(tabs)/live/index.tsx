import { PlaceholderRouteScreen } from "../../../shared/components/placeholder-route-screen";

export default function LiveIndexRoute() {
  return (
    <PlaceholderRouteScreen
      title="Live"
      description="Live route shell is in apps/native now. Feature screens will move in later slices."
    />
  );
}
