import { PlaceholderRouteScreen } from "../../../shared/components/placeholder-route-screen";

export default function FeedIndexRoute() {
  return (
    <PlaceholderRouteScreen
      title="Feed"
      description="Feed route shell is in apps/native now. Feature screens will move in later slices."
    />
  );
}
