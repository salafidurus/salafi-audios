import { ExploreScholarScreen } from "@/features/explore/screens/explore-scholar.screen";

/** Defines the Scholars root entrypoint for the native listener-facing shell. */
/** Renders the scholar directory owned by the first-class Scholars destination. */
export default function ScholarsRoute() {
  return <ExploreScholarScreen />;
}
