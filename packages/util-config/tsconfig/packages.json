{
  "extends": "./base.json",
  "compilerOptions": {
    "module": "ESNext",
    // Bundler resolution allows extensionless imports and package.json "exports" —
    // correct because shared packages are always consumed through a bundler, never run directly by Node
    "moduleResolution": "Bundler",
    "noEmit": true
  }
}
