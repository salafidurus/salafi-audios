{
  "extends": "./app.json",
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "Bundler",
    // "preserve" — Next.js handles JSX transformation via SWC; TS should not transform it
    "jsx": "preserve"
  }
}
