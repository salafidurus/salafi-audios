{
  "extends": "../../tsconfig.packages.json",
  "compilerOptions": {
    "noEmit": true,
    // Project reference support for incremental builds
    "composite": true,
    "incremental": true,
    "types": ["bun-types", "node"]
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist", "**/*.spec.ts", "**/*.test.ts"]
}
