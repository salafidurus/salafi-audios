// Web stub — AdaptiveShell is native-only; web uses Sidebar + TopAuthStrip instead.
import type { RefObject } from "react";
import type { View } from "react-native";

export type AdaptiveShellProps = {
  blurTargetRef?: RefObject<View | null>;
};

export function AdaptiveShell(_props: AdaptiveShellProps) {
  return null;
}
