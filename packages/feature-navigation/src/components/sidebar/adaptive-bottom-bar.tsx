// Fallback stub — used only when the .desktop.web variant is not resolved (e.g. native typecheck).
// At runtime on native, Expo Router Tabs plus CustomTabBar/SubsectionBarHost are used instead.
export function AdaptiveBottomBar() {
  return null;
}
