/** Exposes structured legal policy content without coupling it to a UI stack. */
export {
  legalDocuments,
  legalCompany,
  legalUpdateDates,
  getLegalDocument,
  type LegalBlock,
  type LegalDocument,
  type LegalLocale,
  type LegalSection,
} from "./legal-documents";
