{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    // Bundler — consumed as source by Expo/Next.js bundler, never loaded directly by Node
    "module": "ESNext",
    "moduleResolution": "Bundler"
  },
  "include": ["src"]
}
