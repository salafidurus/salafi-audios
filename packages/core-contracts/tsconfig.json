{
  // Editor / typecheck only — build settings live in tsconfig.build.json.
  "extends": "@sd/util-config/tsconfig/packages.json",
  "compilerOptions": {
    "noEmit": true,
    "moduleSuffixes": [".desktop.web", ".mobile.web", ".native", ".web", ""]
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist"]
}
