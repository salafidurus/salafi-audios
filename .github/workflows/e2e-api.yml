name: e2e-api

on:
  pull_request:
    paths:
      - "apps/api/**"
      - "packages/core-db/prisma/**"
      - "packages/core-db/package.json"
      - "packages/core-contracts/**"
      - "packages/core-i18n/**"
      - "tsconfig*.json"
      - ".bun-version"
      - "bunfig.toml"
      - "package.json"
      - "bun.lock"
      - "turbo.json"
      - ".github/workflows/e2e-api.yml"
    branches:
      - main
      - spec/**

concurrency:
  group: e2e-api-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  setup:
    name: Setup
    outputs:
      branch: ${{ steps.branch_name.outputs.current_branch }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Get branch name
        id: branch_name
        uses: tj-actions/branch-names@v9

  e2e_api:
    name: e2e:api
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version-file: .bun-version

      - name: Set branch expiration (2 hours)
        run: echo "EXPIRES_AT=$(date -u --date '+2 hours' +'%Y-%m-%dT%H:%M:%SZ')" >> "$GITHUB_ENV"

      - name: Create Neon Branch
        id: create_neon_branch
        uses: neondatabase/create-branch-action@v6
        with:
          project_id: ${{ vars.NEON_PROJECT_ID }}
          branch_name: preview/e2e-api-${{ github.event.pull_request.number || github.sha }}-${{ needs.setup.outputs.branch }}
          api_key: ${{ secrets.NEON_API_KEY }}
          expires_at: ${{ env.EXPIRES_AT }}

      - name: Validate Neon Connection
        run: |
          if [ -z "${{ steps.create_neon_branch.outputs.db_url }}" ]; then
            echo "::error::Neon branch database URL is empty"
            exit 1
          fi
          echo "Neon branch created successfully"

      - name: Install dependencies (scripts disabled)
        run: bun install --frozen-lockfile --ignore-scripts

      - name: Build dependencies
        env:
          PRIMARY_DIRECT_DATABASE_URL: ${{ steps.create_neon_branch.outputs.db_url }}
          PRIMARY_DATABASE_URL: ${{ steps.create_neon_branch.outputs.db_url }}
          ANALYTICS_DATABASE_URL: ${{ steps.create_neon_branch.outputs.db_url }}
        run: bun run build:all --filter=api...

      - name: Run Prisma migrations
        env:
          PRIMARY_DIRECT_DATABASE_URL: ${{ steps.create_neon_branch.outputs.db_url }}
          ANALYTICS_DATABASE_URL: ${{ steps.create_neon_branch.outputs.db_url }}
          PRIMARY_DATABASE_URL: ${{ steps.create_neon_branch.outputs.db_url }}
        run: bun run --filter @sd/core-db primary:migrate:deploy

      - name: Run analytics migrations
        env:
          ANALYTICS_DATABASE_URL: ${{ steps.create_neon_branch.outputs.db_url }}
        run: bun run --filter @sd/core-db analytics:migrate:deploy

      - name: Run API E2E Tests
        env:
          PRIMARY_DATABASE_URL: ${{ steps.create_neon_branch.outputs.db_url }}
          PRIMARY_DIRECT_DATABASE_URL: ${{ steps.create_neon_branch.outputs.db_url }}
          ANALYTICS_DATABASE_URL: ${{ steps.create_neon_branch.outputs.db_url }}
          ANALYTICS_IDENTITY_HMAC_SECRET: e2e-analytics-identity-secret-0123456789
          NEON_API_KEY: "e2e-test-neon-api-key"
          NEON_PROJECT_ID: "e2e-test-project"
          NEON_ENDPOINT_ID: "ep-e2e-test-endpoint"
          BETTER_AUTH_SECRET: "e2e-test-secret-must-be-at-least-32-chars-long"
          BETTER_AUTH_URL: "http://localhost:4000"
          CORS_ORIGINS: "http://localhost:3000"
          # Dummy values — E2E tests don't exercise OAuth or R2
          GOOGLE_CLIENT_ID: "dummy"
          GOOGLE_CLIENT_SECRET: "dummy"
          APPLE_CLIENT_ID: "dummy"
          APPLE_CLIENT_SECRET: "dummy"
          R2_ACCOUNT_ID: "dummy"
          R2_ACCESS_KEY_ID: "dummy"
          R2_SECRET_ACCESS_KEY: "dummy"
          R2_BUCKET_NAME: "dummy"
          R2_PUBLIC_BASE_URL: "http://dummy.test"
        run: bun run --filter api test:e2e

      - name: Delete Neon Branch
        if: always()
        uses: neondatabase/delete-branch-action@v3
        with:
          project_id: ${{ vars.NEON_PROJECT_ID }}
          branch: preview/e2e-api-${{ github.event.pull_request.number || github.sha }}-${{ needs.setup.outputs.branch }}
          api_key: ${{ secrets.NEON_API_KEY }}
