name: renovate

on:
  schedule:
    - cron: "0 0 * * *"

  workflow_dispatch:

permissions:
  contents: read

jobs:
  renovate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Self-hosted Renovate
        uses: renovatebot/github-action@v46.1.19
        with:
          configurationFile: .github/renovate-global.json
          token: ${{ secrets.SALAFIDURUS_RENOVATE_TOKEN }}
        env:
          LOG_LEVEL: debug
